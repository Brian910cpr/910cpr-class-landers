# START HERE, CODEX

Brian is remote and may only be able to give you a very short prompt.

When Codex is started on CyberPC, do this immediately:

1. Open `E:\GitHub\910cpr-class-landers`.
2. Pull/fetch `main` safely.
3. Read `ops/handoff/next_task.md` in full.
4. Execute it in order, starting with **P0**.
5. Do not wait for Brian to restate the task in chat.
6. Do not stop after writing scripts. For P0, prove the watcher/dispatcher is actually installed and performs an automatic repo check.
7. Write progress/results back to the existing files in `ops/handoff/`, including the heartbeat.
8. Commit and push meaningful progress so ChatGPT can read your replies remotely.

The current required sequence is:

- **P0**: close the ChatGPT <-> Codex loop with a real recurring CyberPC watcher/dispatcher.
- **P1**: collect the Columbus County 2025 EMS protocol PDFs and publish `https://www.910cpr.com/cc2025ems.zip`.
- **P2**: resume autonomous consumption of eligible LanderWare Production Board work.

If Brian gives you only a prompt like `START HANDOFF`, treat that as authorization to execute this file and `ops/handoff/next_task.md`.

The source of truth for task details is `ops/handoff/next_task.md`.
