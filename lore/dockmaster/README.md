# Dockmaster Lore

Private, reviewed source material for deterministic HTML source comments. Nothing in this directory is customer-facing, and builds must succeed if lore is unavailable.

`ENTRY_LEDGER.json` is the committed stability contract. Entries may be added only after review; generated scheduling and registration behavior must never depend on them.
