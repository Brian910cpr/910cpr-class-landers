const api='/api/schedule-manager/classes',id=new URLSearchParams(location.search).get('id'),root=document.getElementById('workspace');
const esc=v=>{const x=document.createElement('span');x.textContent=String(v??'');return x.innerHTML};let session;
async function request(path,options={}){const response=await fetch(path,options),type=response.headers.get('content-type')||'',body=type.includes('json')?await response.json():null;if(!response.ok)throw new Error(body?.error||`Request failed (${response.status})`);return body}
const nextState=state=>({create:'prepare',prepare:'teach',teach:'close',close:'credential_complete',credential_complete:'archive'})[state];
const studentRow=(person={},index)=>`<div class="roster-row"><input aria-label="Student name" data-name="${index}" value="${esc(person.name)}" placeholder="Student name"><input aria-label="Student email" data-email="${index}" value="${esc(person.email)}" placeholder="Email"><button data-remove="${index}">Remove</button></div>`;
function render(){const w=session.session_workspace,r=session.roster,m=session.requirements_manifest,state=w.lifecycle_state,next=nextState(state),start=new Date(session.start_at||session.start_time);root.innerHTML=`<div class="workspace-head"><div><h1>${esc(session.course_name)}</h1><p class="quiet">${start.toLocaleString()} · ${esc(session.location_name)}</p></div><span class="state">${esc(state.replaceAll('_',' '))}</span></div><div class="toolbar"><a class="button primary" target="_blank" href="${api}/${encodeURIComponent(id)}/packet">Print class packet</a><a class="button" href="${api}/${encodeURIComponent(id)}/packet?download=1">Download packet</a>${next?`<button id="advance">Advance to ${esc(next.replaceAll('_',' '))}</button>`:''}</div><div class="workspace-grid"><section class="panel"><h2>Session summary</h2><dl class="facts"><dt>Instructor</dt><dd>${esc(session.instructor)}</dd><dt>Location</dt><dd>${esc(session.location_name)}</dd><dt>Capacity</dt><dd>${esc(session.capacity)}</dd><dt>Session ID</dt><dd>${esc(session.record_id)}</dd><dt>Manifest policy</dt><dd>${esc(m.policy_version)}</dd></dl></section><section class="panel"><h2>Corporate / client context</h2><p>${esc(w.corporate_client||'No corporate client assigned')}</p><p class="quiet">${esc(w.client_context||'No session-specific client context recorded.')}</p></section></div><section class="panel roster"><h2>Roster</h2><div id="students">${(r.students||[]).map(studentRow).join('')}</div><div class="toolbar"><button id="add-student">Add student</button><button id="save-roster" class="primary">Save roster</button></div><p class="quiet">The printed roster includes ${Math.max(3,r.blank_walk_in_rows||5)} blank walk-in rows. Scanned paperwork can later be linked through Document IDs.</p></section><section class="panel"><h2>Session requirements</h2><p class="quiet">This manifest is preserved for this class. Controlled materials are never embedded in the packet.</p>${m.items.map(item=>`<div class="requirement"><b>${esc(item.requirement)}</b><p class="classification">${esc(item.classification.replaceAll('_',' '))} · Responsible: ${esc(item.responsible_party)}</p><label><input type="checkbox" data-requirement="${esc(item.requirement_id)}" ${item.completed_received?'checked':''}> Completed / received</label>${item.classification!=='landerware_may_provide'?` <button data-ack="${esc(item.requirement_id)}">Acknowledge responsibility</button>`:''}</div>`).join('')}</section>`;document.getElementById('advance')?.addEventListener('click',async()=>{await request(`${api}/${id}/lifecycle`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({target:next})});await load()});document.getElementById('add-student').onclick=()=>{r.students.push({name:'',email:'',source:'registered',attendance_status:'registered',document_ids:[]});render()};document.querySelectorAll('[data-remove]').forEach(button=>button.onclick=()=>{r.students.splice(Number(button.dataset.remove),1);render()});document.getElementById('save-roster').onclick=async()=>{r.students=r.students.map((student,i)=>({...student,name:document.querySelector(`[data-name="${i}"]`).value,email:document.querySelector(`[data-email="${i}"]`).value})).filter(student=>student.name||student.email);await request(`${api}/${id}/roster`,{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({students:r.students})});await load()};document.querySelectorAll('[data-requirement]').forEach(box=>box.onchange=()=>updateRequirement(box.dataset.requirement,{completed_received:box.checked}));document.querySelectorAll('[data-ack]').forEach(button=>button.onclick=()=>updateRequirement(button.dataset.ack,{acknowledged:true}))}
async function updateRequirement(requirement_id,change){await request(`${api}/${id}/requirements`,{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({requirement_id,...change})});await load()}
async function load(){try{session=(await request(`${api}/${encodeURIComponent(id)}`)).class;render()}catch(error){root.innerHTML=`<h1>Session unavailable</h1><p>${esc(error.message)}</p>`}}if(id)load();else root.innerHTML='<h1>Session unavailable</h1><p>No session ID was supplied.</p>';
